doggo.data = data.frame(
  doggoId = c(1:3),
  doggoName = c("Moju", "Happy", "Bella"),
  doggoColor = c("Brown", "White", "Black")
)

print(doggo.data)

doggo.data$doggoChar = c("Tail", "Nose", "Height")

print(doggo.data)